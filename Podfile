# Uncomment the next line to define a global platform for your project
platform :ios, '11.2'

target 'VoiceroidClient' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod "SwiftyJSON", ">= 2.2"

  # Pods for VoiceroidClient

  target 'VoiceroidClientTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VoiceroidClientUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
